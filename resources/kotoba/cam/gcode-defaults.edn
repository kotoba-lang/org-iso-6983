;; Default G-code generation config. Ported verbatim from kami-cam (Rust)
;; `impl Default for GcodeConfig`.
;;
;; Canonical source of the default table — see materials.edn header for why
;; kotoba.cam.gcode keeps a literal copy instead of reading this at runtime,
;; and test/kotoba/cam/materials_edn_test.clj for the drift guard.
[{:db/id -1, :kotoba.cam.gcode-defaults/machine-type :mill-3axis, :kotoba.cam.gcode-defaults/post-processor :fanuc, :kotoba.cam.gcode-defaults/units :millimeters, :kotoba.cam.gcode-defaults/safe-height 5.0, :kotoba.cam.gcode-defaults/coordinate-system :g54, :kotoba.cam.gcode-defaults/program-number 1, :kotoba.cam.gcode-defaults/coolant true, :kotoba.cam.gcode-defaults/default-spindle-rpm 10000}]
